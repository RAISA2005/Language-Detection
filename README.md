# Language-Detection
### 3. Language Detection using RNN  A deep learning-based language detection system built with TensorFlow and Streamlit. The application uses a Simple RNN model to predict the language of user-entered text and displays the prediction along with its confidence score in real time.
